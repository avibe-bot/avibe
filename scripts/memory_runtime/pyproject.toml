[project]
name = "avibe-memory-runtime"
version = "0.1.0"
description = "Pinned dependency input for the Avibe-managed Memory Runtime"
requires-python = ">=3.12,<3.13"
dependencies = [
    "everos==1.1.3",
    "httpx==0.28.1",
    "psutil==7.2.2",
]

[tool.uv]
package = false
